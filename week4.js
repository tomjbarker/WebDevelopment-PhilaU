var my_list = [20, 1003, 394, 2];

for (var x = 0; x < my_list.length; x++){
alert("length of array: " + my_list.length + " value of x: " + x + " value in array: " + my_list[x]); // using x as the index number
}

